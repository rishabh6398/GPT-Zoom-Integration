export default class EndpointApi {
    static validateActions(actions: any): void;
    constructor(options: {}, apiClient: any);
    apiClient: any;
    connectionToEndpointApi: any;
    cache: Cache;
    startEndpoint(config: any): Promise<any>;
    stopEndpoint(config: any): Promise<any>;
    connectToEndpoint(operation: any, endpoint: any, actions: any, intents: any, data: any, connectionId: any, validationToken: any, endpointWebhookUrl: any, headers: any, languages: any): Promise<any>;
}
import Cache from "../cache/Cache";
